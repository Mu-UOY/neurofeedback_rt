Finished blind-phase apparatus (filter-method decision + artificial-simulation validation). Archived
2026-07-18, off the default path, not shipped. See CLEANUP_MANIFEST.md.

`temp/inspect_brainstorm_intro_results.m` was deliberately excluded from this archive move: it was
untracked, gitignored scratch (never committed), so it is not part of this relocation and remains where
it is for separate handling.
